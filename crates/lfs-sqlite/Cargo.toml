[package]
name = "lfs-sqlite"
version.workspace = true
edition.workspace = true
publish.workspace = true
[dependencies]
lfs-core = { path = "../lfs-core" }
lfs-client = { path = "../lfs-client" }
rusqlite.workspace = true
serde_json.workspace = true
[dev-dependencies]
tempfile.workspace = true
