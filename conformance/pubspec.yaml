name: local_sync_conformance
description: Executable conformance fixture for the local-sync Dart runtime.
publish_to: none

environment:
  sdk: ^3.10.3

dependencies:
  local_sync:
    path: ../client/local_sync
  local_sync_database:
    path: ../client/local_sync_database
  local_sync_database_sqlite:
    path: ../client/local_sync_database_sqlite

dev_dependencies:
  sqlite3: ^3.5.0
  test: ^1.25.0
