# Tests

See the [documentation](../website/content/contributing/testing.md) for setup, usage and reference.

Crate tests live beside their Rust modules. `crates/sim` connects the real Rust client and server state machines in one process; see `crates/sim/README.md`.
