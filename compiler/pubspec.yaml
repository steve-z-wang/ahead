name: local_sync_compiler
description: Compiler for local-sync Model definitions.
publish_to: none

environment:
  sdk: ^3.10.3

dependencies:
  dart_style: ^3.1.3
  path: ^1.9.1
  pub_semver: ^2.2.0

dev_dependencies:
  local_sync:
    path: ../client/local_sync
  local_sync_database:
    path: ../client/local_sync_database
  test: ^1.25.0
