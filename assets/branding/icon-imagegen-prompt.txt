Use case: logo-brand / precise-object-edit. Input image is the approved horizontal Ahead logo and is the edit target. Produce a standalone icon extracted from ONLY its left-hand symbol: the red uppercase A with lightly rounded corners and EXACTLY THREE detached horizontal speed bars to its RIGHT. Remove the entire lowercase wordmark 'ahead'. Preserve the approved symbol's silhouette, stroke weight, triangular counter, crossbar, corner rounding, relative bar lengths, spacing and vermilion red. Do not redesign, mirror, rotate or change the motion direction. Center the WHOLE A-plus-three-bars group optically in a SQUARE canvas with generous safe padding, around 12 percent of canvas width at left and right, keeping original group proportions. Background must be genuinely transparent with an alpha channel. Flat solid red, clean antialiased vector-like edges. No text, caption, border, background tile, shadow, gradient, glow or texture. Deliver exactly one standalone icon suitable for a GitHub organization avatar.
